# 进贸通 TradeHub 部署说明

## 你需要装什么

**只需要装 Python**（Node.js 不需要 — 前端已经编译好了）

---

## 第一步：安装 Python（一次，10 分钟）

1. 打开 https://www.python.org/downloads/
2. 点黄色大按钮下载 Python 3.12
3. 双击安装
4. **安装界面第一页，勾选「Add Python to PATH」** ⚠️ 这一步必须勾
5. 点 Install Now

验证：打开终端/命令提示符，输入 `python --version`，显示版本号就对了。

---

## 第二步：拷贝项目（一次，2 分钟）

打开终端，把 tradehub 文件夹拷贝进来，放在不用删的地方，比如 `D:\tradehub`。

```bash
# 进入项目目录
cd D:\tradehub

# 安装依赖（只需做一次）
pip install -r backend/requirements.txt
```

---

## 第三步：启动（每次使用 3 秒）

```bash
cd D:\tradehub
python backend/main.py --production
```

看到下面这行就成功了：
```
✅ 生产模式：前端已挂载
打开 http://localhost:8890 即可使用
```

浏览器打开 `http://localhost:8890`

---

## 日常使用

| 操作 | 方法 |
|------|------|
| 启动 | 终端输入 `cd D:\tradehub && python backend/main.py --production` |
| 停止 | 关掉终端窗口 |
| 备份 | 复制 `backend/data/tradehub.db` 到 U 盘 / 网盘 |
| 恢复 | 把备份的 `tradehub.db` 放回原位置 |

---

## 数据在哪

- 数据库文件：`backend/data/tradehub.db`（一个文件）
- 合同 PDF：`backend/data/contracts/`
- 备份只需要复制 `tradehub.db` 这个文件

---

## 局域网多人使用

如果公司两三台电脑都想用：

1. 在主机上启动：`python backend/main.py --production`
2. 其他电脑浏览器输入 `http://主机IP:8890`
3. Windows 需要防火墙允许 8890 端口

查主机 IP：终端输入 `ipconfig`（Windows）或 `ifconfig | grep inet`（Mac）

---

## 重置示例数据

```bash
cd D:\tradehub
python backend/seed.py
```

这会清空所有数据并灌入新的示例数据。

---

## 常见问题

| 问题 | 解决 |
|------|------|
| `python: command not found` | 第一步没勾「Add Python to PATH」，重装 Python |
| `ModuleNotFoundError: No module named 'fastapi'` | 没装依赖，执行 `pip install -r backend/requirements.txt` |
| 页面打开白屏 | 前端没构建，执行 `cd frontend && npm install && npm run build`（需要装 Node.js） |
| 端口被占用 | 换端口：`python backend/main.py --production` 前加 `TRADEHUB_PORT=8891` |
